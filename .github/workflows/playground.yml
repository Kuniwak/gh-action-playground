name: Playground for echo-inputs-json
on: push
jobs:
  Playground:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: ./action.yml
        with:
          foo: "FOOFOO"
          baz: "BAZBAZ"

